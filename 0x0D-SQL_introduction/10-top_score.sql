-- Script lists all records of second_table (hbtn_0c_0)
-- Results shold display both the score and the name
-- Records should be ordered by score top first
SELECT `score`, `name`
FROM `second_table`
ORDER BY `score` DESC;
